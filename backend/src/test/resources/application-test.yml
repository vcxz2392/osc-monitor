app:
  # 시뮬레이터가 배경에서 돌면 다른 테스트의 기대값이 흔들린다.
  # 필요한 테스트만 @TestPropertySource 로 켜고 직접 호출한다.
  simulator:
    enabled: false
spring:
  jpa:
    properties:
      # 조회 한 번에 쿼리가 몇 개 나가는지 테스트가 세기 위해 켠다.
      hibernate:
        generate_statistics: true
